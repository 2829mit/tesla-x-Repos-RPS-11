
import React from 'react';

// This component is neutralized as RFID options are removed
interface ComparisonModalProps {
  onClose: () => void;
  showPrices: boolean;
}

const ComparisonModal: React.FC<ComparisonModalProps> = ({ onClose }) => {
  return null;
};

export default ComparisonModal;
